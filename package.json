{
  "name": "live-tracker",
  "version": "0.1.0",
  "homepage": "https://ijmacd.github.io/live-tracker/",
  "private": true,
  "dependencies": {
    "moment-timezone": "^0.5.14",
    "react": "^17",
    "react-dom": "^17",
    "react-scripts": "^4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
